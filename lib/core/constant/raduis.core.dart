import 'package:flutter/material.dart';

/// borderRadius all 3
const circular3 = BorderRadius.all(Radius.circular(3));

/// borderRadius all 4

const circular4 = BorderRadius.all(Radius.circular(4));

/// borderRadius all 6
const circular6 = BorderRadius.all(Radius.circular(6));

/// borderRadius all 8
const circular8 = BorderRadius.all(Radius.circular(8));

/// borderRadius all 12
const circular12 = BorderRadius.all(Radius.circular(12));

/// borderRadius all 14
const circular14 = BorderRadius.all(Radius.circular(14));

/// borderRadius all 16
const circular16 = BorderRadius.all(Radius.circular(16));

/// borderRadius all 20
const circular20 = BorderRadius.all(Radius.circular(20));

/// borderRadius all 24
const circular24 = BorderRadius.all(Radius.circular(24));

/// borderRadius all 32
const circular32 = BorderRadius.all(Radius.circular(32));

/// borderRadius all 40
const circular40 = BorderRadius.all(Radius.circular(40));

/// borderRadius all 360
const circular360 = BorderRadius.all(Radius.circular(32));
